import React from "react"
import AccountPage, { getServerSideProps } from "src/pages/account"

describe("The account page", () => {
  it("getServerSideProps return Date and predicate isAdmin equal boolean from token", () => {

  })
})
